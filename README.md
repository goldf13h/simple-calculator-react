# Simple React Calculator

Minimal React calculator using Vite.

Quick start (Windows PowerShell):

```powershell
cd "C:\Users\8963964\Downloads\extra\251118 react"
npm install
npm run dev
``` 

Open the dev URL shown by Vite (default http://localhost:5173).
